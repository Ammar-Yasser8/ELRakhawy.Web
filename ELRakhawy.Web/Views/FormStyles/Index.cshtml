@using ELRakhawy.EL.ViewModels
@model IEnumerable<FormStyleListViewModel>
@{
    ViewData["Title"] = "اﻟﻮاﺟﻬﺎت اﻟﻤﺮﺗﺒﻄﺔ";
    var currentTime = "2025-08-19 20:46:08";
    var currentUser = "Ammar-Yasser8";
}

<!-- Enhanced Navigation Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-4">
    <div class="card border-0 shadow-sm">
        <div class="card-body py-2">
            <ol class="breadcrumb mb-0 align-items-center">
                <li class="breadcrumb-item">
                    <a href="@Url.Action("Index", "Home")" class="text-decoration-none d-flex align-items-center">
                        <i class="fas fa-home text-primary me-2"></i>
                        <span class="fw-semibold">الرئيسية</span>
                    </a>
                </li>
                <li class="breadcrumb-item">
                    <a href="@Url.Action("PublicTables", "Home")" class="text-decoration-none d-flex align-items-center">
                        <i class="fas fa-table text-info me-2"></i>
                        <span class="fw-semibold">الجداول العامة</span>
                    </a>
                </li>
                <li class="breadcrumb-item active d-flex align-items-center">
                    <i class="fas fa-cogs text-warning me-2"></i>
                    <span class="fw-bold text-dark">@ViewData["Title"]</span>
                </li>
            </ol>
        </div>
    </div>
</nav>

<!-- Enhanced Header Section -->
<div class="welcome-card mb-4">
    <div class="card border-0 shadow-sm">
        <div class="card-body bg-light p-4">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <div class="d-flex align-items-center">
                        <div class="icon-circle bg-warning text-white me-3">
                            <i class="fas fa-cogs"></i>
                        </div>
                        <div>
                            <h2 class="mb-1 text-dark">إدارة اﻟﻮاﺟﻬﺎت اﻟﻤﺮﺗﺒﻄﺔ</h2>
                            <p class="text-muted mb-0">
                                <i class="fas fa-info-circle me-1"></i>
                                عرض وإدارة  اﻟﻮاﺟﻬﺎت اﻟﻤﺮﺗﺒﻄﺔ المستخدمة في أنماط التعبئة
                            </p>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 text-lg-end mt-3 mt-lg-0">
                    <div class="d-flex flex-column flex-lg-row gap-2 justify-content-lg-end">
                        <button class="btn btn-outline-info" id="refreshData">
                            <i class="fas fa-sync-alt me-1"></i>تحديث البيانات
                        </button>
                        <a asp-action="Create" class="btn btn-success">
                            <i class="fas fa-plus-circle me-1"></i>إضافة وجهة جديدة
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Enhanced Success/Error Messages -->
@if (TempData["Success"] != null || TempData["Error"] != null)
{
    <div class="alert @(TempData["Success"] != null ? "alert-success" : "alert-danger") alert-dismissible fade show shadow-sm border-0" role="alert">
        <div class="d-flex align-items-center">
            <i class="fas @(TempData["Success"] != null ? "fa-check-circle" : "fa-exclamation-circle") me-3 fa-lg"></i>
            <div>
                <strong>@(TempData["Success"] != null ? "نجح!" : "خطأ!")</strong>
                <span class="ms-2">@(TempData["Success"] ?? TempData["Error"])</span>
            </div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<!-- Enhanced Data Display Section with Inline Search -->
<div id="formStylesContainer">
    <div class="section-card">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-light border-bottom">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="mb-0 fw-bold text-dark">
                        <i class="fas fa-list me-2 text-primary"></i>قائمة العمليات التجارية
                    </h5>
                    <div class="d-flex align-items-center gap-3">
                        <!-- Search Results Info -->
                        <small class="text-muted">
                            <span id="resultsInfo">عرض <strong id="visibleCount">0</strong> من أصل <strong id="totalCount">0</strong> عملية</span>
                        </small>
                        <div class="badge bg-warning px-3 py-2">
                            <i class="fas fa-database me-1"></i>
                            <span id="badgeCount">0</span> عملية
                        </div>
                        <button class="btn btn-outline-danger btn-sm" id="clearAllSearch" style="display: none;">
                            <i class="fas fa-eraser me-1"></i>مسح المرشحات
                        </button>
                    </div>
                </div>
            </div>

            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0" id="formStylesTable">
                        <thead class="table-light">
                            <tr>
                                <th class="fw-bold" style="width: 50%;">
                                    <i class="fas fa-cogs text-primary me-1"></i>اسم الواجهة 
                                </th>
                               
                                <th class="text-center fw-bold" style="width: 15%;">
                                    <i class="fas fa-tools text-dark me-1"></i>الإجراءات
                                </th>
                            </tr>
                            <!-- Enhanced Inline Search Row -->
                            <tr class="search-row bg-white">
                                <th class="border-bottom-0 pb-2 pt-2">
                                    <div class="search-container">
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light border-end-0">
                                                <i class="fas fa-search text-muted"></i>
                                            </span>
                                            <input type="text" id="searchByFormName" class="form-control border-start-0"
                                                   placeholder="ابحث في أسماء العمليات...">
                                            <button class="btn btn-outline-secondary btn-sm" type="button" id="clearFormNameSearch" style="display: none;">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </th>
                                
                                <th class="border-bottom-0 pb-2 pt-2 text-center">
                                    <div class="d-flex gap-1 justify-content-center">
                                        <button class="btn btn-sm btn-outline-primary" id="exportResults" title="تصدير النتائج">
                                            <i class="fas fa-download"></i>
                                        </button>
                                        <button class="btn btn-sm btn-outline-info" id="toggleColumns" title="إظهار/إخفاء الأعمدة">
                                            <i class="fas fa-columns"></i>
                                        </button>
                                    </div>
                                </th>
                            </tr>
                        </thead>
                        <tbody id="formStylesTableBody">
                            @if (Model != null && Model.Any())
                            {
                                @foreach (var form in Model)
                                {
                                    <tr class="form-style-row" data-id="@form.Id" data-category="@form.FormCategory">
                                        <td class="form-style-name">
                                            <div class="d-flex align-items-center">
                                                <div class="form-icon me-2">
                                                    <i class="fas fa-cog text-warning"></i>
                                                </div>
                                                <div>
                                                    <span class="fw-semibold">@form.FormName</span>
                                                    <br>
                                                   
                                                </div>
                                            </div>
                                        </td>
                                       
                                        <td class="text-center">
                                            <div class="btn-group" role="group">
                                                <a asp-action="Details" asp-route-id="@form.Id"
                                                   class="btn btn-sm btn-outline-info"
                                                   title="تفاصيل @form.FormName">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a asp-action="Edit" asp-route-id="@form.Id"
                                                   class="btn btn-sm btn-outline-warning"
                                                   title="تعديل @form.FormName">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <button class="btn btn-sm @(form.UsageCount > 0 ? "btn-outline-secondary" : "btn-outline-danger") delete-btn"
                                                        data-id="@form.Id"
                                                        data-name="@form.FormName"
                                                        data-usage="@form.UsageCount"
                                                        title="@(form.UsageCount > 0 ? "لا يمكن الحذف" : "حذف") @form.FormName"
                                                        @(form.UsageCount > 0 ? "disabled" : "")>
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- No Results Message -->
    <div id="noResultsMessage" class="text-center py-5" style="display: none;">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <i class="fas fa-search fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">لا توجد نتائج مطابقة</h5>
                <p class="text-muted mb-3">لم يتم العثور على عمليات تجارية تطابق معايير البحث الحالية</p>
                <button class="btn btn-outline-primary" id="resetSearch">
                    <i class="fas fa-refresh me-1"></i>إعادة تعيين البحث
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Enhanced Mobile View -->
<div class="d-lg-none mt-4" id="mobileView">
    <!-- Mobile Search Controls -->
    <div class="card border-0 shadow-sm mb-3">
        <div class="card-body p-3">
            <div class="row g-2">
                <div class="col-12">
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="fas fa-search text-muted"></i>
                        </span>
                        <input type="text" id="searchInputMobile" class="form-control"
                               placeholder="ابحث في العمليات...">
                        <button class="btn btn-outline-secondary" type="button" id="clearMobileSearch" style="display: none;">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                </div>
                <div class="col-6">
                    <select id="mobileCategoryFilter" class="form-select form-select-sm">
                        <option value="">جميع الفئات</option>
                    </select>
                </div>
                <div class="col-6">
                    <select id="mobileUsageFilter" class="form-select form-select-sm">
                        <option value="">جميع الحالات</option>
                        <option value="used">مستخدم</option>
                        <option value="unused">غير مستخدم</option>
                    </select>
                </div>
            </div>
        </div>
    </div>

    <!-- Mobile Cards -->
    <div id="mobileCardsContainer">
        @if (Model != null && Model.Any())
        {
            @foreach (var form in Model)
            {
                <div class="card mb-3 mobile-form-card" data-id="@form.Id" data-category="@form.FormCategory" data-usage="@form.UsageCount">
                    <div class="card-header d-flex justify-content-between align-items-center py-3">
                        <div class="d-flex align-items-center">
                            <i class="fas fa-cog text-warning me-2"></i>
                            <h6 class="mb-0 fw-bold">@form.FormName</h6>
                        </div>
                        <div class="dropdown">
                            <button class="btn btn-sm btn-light" type="button" data-bs-toggle="dropdown">
                                <i class="fas fa-ellipsis-v"></i>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li>
                                    <a class="dropdown-item py-2" asp-action="Details" asp-route-id="@form.Id">
                                        <i class="fas fa-eye me-2"></i>تفاصيل
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item py-2" asp-action="Edit" asp-route-id="@form.Id">
                                        <i class="fas fa-edit me-2"></i>تعديل
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                @if (form.UsageCount > 0)
                                {
                                    <li>
                                        <span class="dropdown-item py-2 text-secondary">
                                            <i class="fas fa-ban me-2"></i>لا يمكن الحذف (مستخدم)
                                        </span>
                                    </li>
                                }
                                else
                                {
                                    <li>
                                        <a class="dropdown-item py-2 text-danger mobile-delete-btn"
                                           data-id="@form.Id" data-name="@form.FormName" href="#!">
                                            <i class="fas fa-trash me-2"></i>حذف
                                        </a>
                                    </li>
                                }
                            </ul>
                        </div>
                    </div>
                    <div class="card-body py-3">
                        <div class="row g-2">
                            <div class="col-6">
                                <div class="text-center p-2 bg-light rounded">
                                    <div class="badge bg-info mb-1">@form.FormCategory</div>
                                    <div><small class="text-muted">الفئة</small></div>
                                </div>
                            </div>
                            <div class="col-6">
                                <div class="text-center p-2 bg-light rounded">
                                    <div class="badge @(form.UsageCount > 0 ? "bg-success" : "bg-secondary") mb-1">@form.UsageCount</div>
                                    <div><small class="text-muted">الاستخدامات</small></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>

<!-- Enhanced Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-danger text-white border-bottom-0">
                <h5 class="modal-title fw-bold">
                    <i class="fas fa-exclamation-triangle me-2"></i>تأكيد حذف العملية التجارية
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-4">
                <div class="text-center">
                    <i class="fas fa-trash-alt fa-3x text-danger mb-3"></i>
                    <h6 class="mb-3">هل أنت متأكد من حذف هذه العملية؟</h6>
                    <div class="alert alert-warning">
                        <strong id="deleteFormName" class="text-dark"></strong>
                    </div>
                    <div class="alert alert-danger">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>تحذير:</strong> هذا الإجراء لا يمكن التراجع عنه!
                    </div>
                </div>
            </div>
            <div class="modal-footer border-top">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-2"></i>إلغاء
                </button>
                <button type="button" class="btn btn-danger" id="confirmDelete">
                    <i class="fas fa-trash me-2"></i>تأكيد الحذف
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            const CURRENT_TIME = '2025-09-04 16:12:35';
            const CURRENT_USER = 'Ammar-Yasser8';

            console.log('Enhanced form styles index with Arabic numbers loaded:', {
                time: CURRENT_TIME,
                user: CURRENT_USER
            });

            // Arabic number conversion functions
            function toArabicDigits(str) {
                if (typeof str !== 'string') return str;
                return str.replace(/[0-9]/g, d => '٠١٢٣٤٥٦٧٨٩'[d]);
            }

            function toLatinDigits(str) {
                if (typeof str !== 'string') return str;
                return str.replace(/[٠-٩]/g, d => '0123456789'['٠١٢٣٤٥٦٧٨٩'.indexOf(d)]);
            }

            function isArabicChar(c) {
                return /[\u0600-\u06FF]/.test(c);
            }

            function isEnglishChar(c) {
                return /[a-zA-Z]/.test(c);
            }

            function getLastLetter(str) {
                if (!str || str.trim().length === 0) {
                    return 'arabic';
                }

                for (let i = str.length - 1; i >= 0; i--) {
                    let char = str[i];
                    if (isArabicChar(char)) {
                        return 'arabic';
                    } else if (isEnglishChar(char)) {
                        return 'english';
                    }
                }
                return 'arabic'; // default is Arabic
            }

            // Apply Arabic number conversion to search inputs
            function applyNumberConversion($input) {
                let val = $input.val();

                if (val && val.length > 0) {
                    let lastLetterLang = getLastLetter(val);
                    let newVal = lastLetterLang === 'arabic' ? toArabicDigits(val) : toLatinDigits(val);

                    if (newVal !== val) {
                        let cursorPos = $input[0].selectionStart;
                        $input.val(newVal);
                        try {
                            $input[0].setSelectionRange(cursorPos, cursorPos);
                        } catch (ex) {
                            $input.focus();
                        }
                    }
                }
            }

            // Enhanced search input event handler with Arabic support
            $(document).on("input keyup paste", "#searchByFormName, #searchInputMobile, #mobileCategoryFilter, #mobileUsageFilter", function(e) {
                const $input = $(this);

                // Apply number conversion for text inputs only
                if ($input.attr('type') === 'text' || $input.is('input:not([type])')) {
                    applyNumberConversion($input);
                }
            });

            // Direct number key conversion for immediate Arabic conversion
            $(document).on("keypress", "#searchByFormName, #searchInputMobile", function(e) {
                // Convert English digits to Arabic immediately
                if (e.which >= 48 && e.which <= 57) { // 0-9
                    e.preventDefault();
                    let digit = String.fromCharCode(e.which);
                    let arabicDigit = toArabicDigits(digit);

                    let input = e.target;
                    let cursorPos = input.selectionStart || 0;
                    let currentValue = input.value;

                    let newValue = currentValue.slice(0, cursorPos) + arabicDigit + currentValue.slice(cursorPos);
                    input.value = newValue;

                    input.setSelectionRange(cursorPos + 1, cursorPos + 1);

                    // Trigger search
                    $(input).trigger('input');
                }
            });

            // Initialize components
            const deleteModal = new bootstrap.Modal(document.getElementById('deleteModal'));
            let currentDeleteId = null;

            // Enhanced search functionality with Arabic number support
            function performSearch() {
                const formNameSearch = $('#searchByFormName').val().toLowerCase().trim();
                const formNameSearchLatin = toLatinDigits(formNameSearch);
                const formNameSearchArabic = toArabicDigits(formNameSearch);

                const categoryFilter = $('#searchByCategory').val();
                const usageFilter = $('#searchByUsage').val();
                let visibleCount = 0;

                $('.form-style-row').each(function() {
                    const row = $(this);
                    const formName = row.find('.form-style-name').text().toLowerCase();
                    const category = row.data('category');
                    const usage = parseInt(row.find('.form-style-usage').data('usage')) || 0;

                    // Enhanced search matching with Arabic/English number support
                    const formNameMatch = !formNameSearch ||
                        formName.includes(formNameSearch) ||
                        formName.includes(formNameSearchLatin) ||
                        formName.includes(formNameSearchArabic);

                    const categoryMatch = !categoryFilter || category === categoryFilter;

                    let usageMatch = true;
                    if (usageFilter) {
                        switch(usageFilter) {
                            case 'used':
                                usageMatch = usage > 0;
                                break;
                            case 'unused':
                                usageMatch = usage === 0;
                                break;
                            case 'high':
                                usageMatch = usage >= 5;
                                break;
                            case 'medium':
                                usageMatch = usage >= 1 && usage <= 4;
                                break;
                        }
                    }

                    const shouldShow = formNameMatch && categoryMatch && usageMatch;
                    row.toggle(shouldShow);

                    if (shouldShow) visibleCount++;
                });

                updateSearchInfo(visibleCount, formNameSearch, categoryFilter, usageFilter);
            }

            // UPDATED: Enhanced updateSearchInfo without empty state display
            function updateSearchInfo(visibleCount, formNameSearch, categoryFilter, usageFilter) {
                // Display counts in Arabic numerals
                $('#visibleCount').text(toArabicDigits(visibleCount.toString()));
                $('#badgeCount').text(toArabicDigits(visibleCount.toString()));

                // Show/hide clear buttons
                $('#clearFormNameSearch').toggle(formNameSearch.length > 0);

                const hasAnyFilter = formNameSearch.length > 0 || categoryFilter || usageFilter;
                $('#clearAllSearch').toggle(hasAnyFilter);

                // REMOVED: Empty state display - just show/hide no results message
                if (visibleCount === 0 && hasAnyFilter) {
                    $('#noResultsMessage').show();
                    // Keep the table visible always
                    $('#formStylesContainer .section-card').show();
                    console.log('No results found for search criteria - showing no results message only');
                } else {
                    $('#noResultsMessage').hide();
                    $('#formStylesContainer .section-card').show();
                }

                // Add visual feedback to search inputs
                $('#searchByFormName').toggleClass('border-primary', formNameSearch.length > 0);

                console.log(`Search completed: ${visibleCount} results for "${formNameSearch}"`);
            }

            // Enhanced updateCounts with Arabic numbers
            function updateCounts() {
                const totalCount = $('.form-style-row').length;
                const visibleCount = $('.form-style-row:visible').length;

                $('#totalCount').text(toArabicDigits(totalCount.toString()));
                $('#visibleCount').text(toArabicDigits(visibleCount.toString()));
                $('#badgeCount').text(toArabicDigits(visibleCount.toString()));
            }

            function populateFilterOptions() {
                // Populate categories
                const categories = [...new Set($('.form-style-row').map(function() {
                    return $(this).data('category');
                }).get())].filter(cat => cat).sort();

                categories.forEach(category => {
                    $('#searchByCategory, #mobileCategoryFilter').append(
                        `<option value="${category}">${category}</option>`
                    );
                });
            }

            // Search events with debouncing
            let searchTimeout;
            $('#searchByFormName, #searchByCategory, #searchByUsage').on('input change', function() {
                clearTimeout(searchTimeout);
                searchTimeout = setTimeout(performSearch, 200);
            });

            // Enhanced mobile search with Arabic number support
            $('#searchInputMobile, #mobileCategoryFilter, #mobileUsageFilter').on('input change', function() {
                const searchTerm = $('#searchInputMobile').val().toLowerCase();
                const searchTermLatin = toLatinDigits(searchTerm);
                const searchTermArabic = toArabicDigits(searchTerm);

                const categoryFilter = $('#mobileCategoryFilter').val();
                const usageFilter = $('#mobileUsageFilter').val();
                let visibleMobileCount = 0;

                $('.mobile-form-card').each(function() {
                    const card = $(this);
                    const text = card.text().toLowerCase();
                    const category = card.data('category');
                    const usage = parseInt(card.data('usage')) || 0;

                    // Enhanced mobile search with Arabic/English number support
                    const textMatch = !searchTerm ||
                        text.includes(searchTerm) ||
                        text.includes(searchTermLatin) ||
                        text.includes(searchTermArabic);

                    const categoryMatch = !categoryFilter || category === categoryFilter;

                    let usageMatch = true;
                    if (usageFilter) {
                        switch(usageFilter) {
                            case 'used':
                                usageMatch = usage > 0;
                                break;
                            case 'unused':
                                usageMatch = usage === 0;
                                break;
                        }
                    }

                    const shouldShow = textMatch && categoryMatch && usageMatch;
                    card.toggle(shouldShow);

                    if (shouldShow) visibleMobileCount++;
                });

                $('#clearMobileSearch').toggle($('#searchInputMobile').val().length > 0);

                console.log(`Mobile search: ${visibleMobileCount} cards visible`);
            });

            // Clear search buttons
            $('#clearFormNameSearch').click(function() {
                $('#searchByFormName').val('').removeClass('border-primary');
                performSearch();
                $('#searchByFormName').focus();
            });

            $('#clearAllSearch, #resetSearch').click(function() {
                $('#searchByFormName, #searchByCategory, #searchByUsage').val('').removeClass('border-primary');
                performSearch();
            });

            $('#clearMobileSearch').click(function() {
                $('#searchInputMobile').val('');
                $('#searchInputMobile').trigger('input');
            });

            // Enhanced delete functionality with Arabic number support
            $(document).on('click', '.delete-btn, .mobile-delete-btn', function(e) {
                e.preventDefault();
                const id = $(this).data('id');
                const name = $(this).data('name');
                const usage = parseInt($(this).data('usage')) || 0;

                if (usage > 0) {
                    const arabicUsage = toArabicDigits(usage.toString());
                    showNotification(`لا يمكن حذف هذه العملية لأنها مستخدمة في ${arabicUsage} من أنماط التعبئة`, 'warning');
                    return;
                }

                currentDeleteId = id;
                $('#deleteFormName').text(name);
                deleteModal.show();
            });

            $('#confirmDelete').click(function() {
                if (!currentDeleteId) return;

                const btn = $(this);
                const originalText = btn.html();
                btn.prop('disabled', true)
                   .html('<span class="spinner-border spinner-border-sm me-2"></span>جارٍ الحذف...');

                $.ajax({
                    url: `/FormStyles/${currentDeleteId}`,
                    method: 'DELETE',
                    success: function(response) {
                        if (response.success) {
                            deleteModal.hide();
                            $(`.form-style-row[data-id="${currentDeleteId}"]`).fadeOut(400, function() {
                                $(this).remove();
                                updateCounts();
                                performSearch();
                            });
                            $(`.mobile-form-card[data-id="${currentDeleteId}"]`).fadeOut(400, function() {
                                $(this).remove();
                            });

                            showNotification(response.message, 'success');
                        } else {
                            btn.prop('disabled', false).html(originalText);
                            showNotification(response.message || 'حدث خطأ أثناء الحذف', 'error');
                        }
                        currentDeleteId = null;
                    },
                    error: function(xhr) {
                        btn.prop('disabled', false).html(originalText);
                        const errorMessage = xhr.responseJSON?.message || 'حدث خطأ أثناء حذف العملية';
                        showNotification(errorMessage, 'error');
                        currentDeleteId = null;
                    }
                });
            });

            // Enhanced export functionality with Arabic numbers
            $('#exportResults').click(function() {
                const visibleRows = $('.form-style-row:visible');
                if (visibleRows.length === 0) {
                    showNotification('لا توجد بيانات للتصدير', 'warning');
                    return;
                }

                let csvContent = "اسم العملية,الفئة,الاستخدامات\n";
                visibleRows.each(function() {
                    const name = $(this).find('.form-style-name .fw-semibold').text();
                    const category = $(this).data('category') || 'غير محدد';
                    const usage = parseInt($(this).data('usage')) || 0;
                    const arabicUsage = toArabicDigits(usage.toString());
                    csvContent += `"${name}","${category}","${arabicUsage}"\n`;
                });

                const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
                const link = document.createElement('a');
                link.href = URL.createObjectURL(blob);
                link.download = `business_operations_${new Date().toISOString().split('T')[0]}.csv`;
                link.click();

                const arabicCount = toArabicDigits(visibleRows.length.toString());
                showNotification(`تم تصدير ${arabicCount} عملية بنجاح`, 'success');
            });

            // Refresh data
            $('#refreshData').click(function() {
                const btn = $(this);
                const originalText = btn.html();
                btn.prop('disabled', true).html('<span class="spinner-border spinner-border-sm me-2"></span>جاري التحديث...');

                setTimeout(() => {
                    location.reload();
                }, 1000);
            });

            // Enhanced notification function
            function showNotification(message, type = 'success') {
                const alertTypes = {
                    'success': { class: 'alert-success', icon: 'fa-check-circle' },
                    'error': { class: 'alert-danger', icon: 'fa-exclamation-circle' },
                    'warning': { class: 'alert-warning', icon: 'fa-exclamation-triangle' },
                    'info': { class: 'alert-info', icon: 'fa-info-circle' }
                };

                const alertConfig = alertTypes[type] || alertTypes['info'];

                const alert = $(`
                    <div class="alert ${alertConfig.class} alert-dismissible fade show shadow-sm border-0" role="alert">
                        <div class="d-flex align-items-center">
                            <i class="fas ${alertConfig.icon} me-3 fa-lg"></i>
                            <span>${message}</span>
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                `);

                $('.welcome-card').after(alert);

                setTimeout(() => {
                    alert.fadeOut(350, function() { $(this).remove(); });
                }, 4000);
            }

            // Auto-dismiss success alerts after 5 seconds
            setTimeout(function() {
                $('.alert-success, .alert-danger').fadeOut(350);
            }, 5000);

            // Keyboard shortcuts
            $(document).keydown(function(e) {
                if (e.ctrlKey && e.keyCode === 70) { // Ctrl+F
                    e.preventDefault();
                    $('#searchByFormName').focus();
                }
                if (e.keyCode === 27) { // Escape
                    $('#searchByFormName, #searchByCategory, #searchByUsage').val('').removeClass('border-primary');
                    performSearch();
                }
            });

            // Test number conversion functions
            console.log('Testing Arabic number conversion:');
            console.log('123 to Arabic:', toArabicDigits('123'));
            console.log('١٢٣ to Latin:', toLatinDigits('١٢٣'));
            console.log('Test text with 123:', toArabicDigits('نص تجريبي 123'));

            // Initialize with Arabic numbers
            populateFilterOptions();
            updateCounts();

            console.log('Enhanced form styles index with Arabic numbers initialized successfully at', CURRENT_TIME, 'by', CURRENT_USER);
        });
    </script>
    <style>
        /* Enhanced Styles */
        .icon-circle {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .form-style-row {
            transition: all 0.2s ease;
        }

            .form-style-row:hover {
                background-color: rgba(255,193,7,0.05);
                transform: scale(1.002);
            }

        .search-row th {
            background-color: #ffffff;
            border-bottom: 1px solid #dee2e6;
            padding: 8px 12px;
        }

        .search-container .input-group {
            transition: all 0.2s ease;
        }

            .search-container .input-group:hover {
                transform: translateY(-1px);
            }

        .search-container .input-group-text {
            background-color: #f8f9fa;
            border-color: #ced4da;
            font-size: 0.875rem;
        }

        .search-container .form-control, .search-container .form-select {
            border-color: #ced4da;
            font-size: 0.875rem;
            padding: 6px 12px;
        }

            .search-container .form-control:focus, .search-container .form-select:focus {
                border-color: #86b7fe;
                box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
            }

            .search-container .form-control.border-primary {
                border-color: #0d6efd !important;
                box-shadow: 0 0 0 0.1rem rgba(13, 110, 253, 0.15);
            }

        .form-icon {
            width: 30px;
            text-align: center;
        }

        .btn-group .btn {
            margin: 0 1px;
        }

        .mobile-form-card {
            transition: all 0.2s ease;
            border-left: 4px solid transparent;
        }

            .mobile-form-card:hover {
                border-left-color: #ffc107;
                box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            }

        .form-style-row {
            animation: fadeInUp 0.5s ease-out;
        }

        @@keyframes fadeInUp {
            from

        {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .table thead th {
            position: relative;
            vertical-align: middle;
        }

        .table thead tr:first-child th {
            border-bottom: 2px solid #dee2e6;
        }

        /* Mobile responsiveness */
        @@media (max-width: 768px) {
            .icon-circle

        {
            width: 45px;
            height: 45px;
            font-size: 1.2rem;
        }

        .btn-group {
            flex-direction: column;
            width: 100%;
        }

            .btn-group .btn {
                margin: 1px 0;
                border-radius: 4px !important;
            }

        .search-row th {
            padding: 6px 8px;
        }

        .search-container .form-control, .search-container .form-select {
            font-size: 0.8rem;
            padding: 4px 8px;
        }

        .mobile-form-card {
            margin-bottom: 1rem;
        }

        }

        /* Print styles */
        @@media print {
            .btn-group, .search-row, .btn

        {
            display: none !important;
        }

        .form-style-row {
            break-inside: avoid;
        }

        }
    </style>
}
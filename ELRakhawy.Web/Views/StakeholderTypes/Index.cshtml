@using ELRakhawy.EL.Models
@model IEnumerable<StakeholderType>
@{
    ViewData["Title"] = "أنواع أصحاب المصلحة";
    var currentTime = "2025-08-19 19:31:55";
    var currentUser = "Ammar-Yasser8";
}

<!-- Enhanced Navigation Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-4">
    <div class="card border-0 shadow-sm">
        <div class="card-body py-2">
            <ol class="breadcrumb mb-0 align-items-center">
                <li class="breadcrumb-item">
                    <a href="@Url.Action("Index", "Home")" class="text-decoration-none d-flex align-items-center">
                        <i class="fas fa-home text-primary me-2"></i>
                        <span class="fw-semibold">الرئيسية</span>
                    </a>
                </li>
                <li class="breadcrumb-item">
                    <a href="@Url.Action("PublicTables", "Home")" class="text-decoration-none d-flex align-items-center">
                        <i class="fas fa-table text-info me-2"></i>
                        <span class="fw-semibold">الجداول العامة</span>
                    </a>
                </li>
                <li class="breadcrumb-item active d-flex align-items-center">
                    <i class="fas fa-user-tag text-warning me-2"></i>
                    <span class="fw-bold text-dark">@ViewData["Title"]</span>
                </li>
            </ol>
        </div>
    </div>
</nav>

<!-- Enhanced Header Section -->
<div class="welcome-card mb-4">
    <div class="card border-0 shadow-sm">
        <div class="card-body bg-light p-4">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <div class="d-flex align-items-center">
                        <div class="icon-circle bg-warning text-white me-3">
                            <i class="fas fa-user-tag"></i>
                        </div>
                        <div>
                            <h2 class="mb-1 text-dark">إدارة أنواع أصحاب المصلحة</h2>
                            <p class="text-muted mb-0">
                                <i class="fas fa-info-circle me-1"></i>
                                عرض وإدارة أنواع أصحاب المصلحة والواجهات المرتبطة بهم
                            </p>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 text-lg-end mt-3 mt-lg-0">
                    <div class="d-flex flex-column flex-lg-row gap-2 justify-content-lg-end">
                        <button class="btn btn-outline-info" id="refreshData">
                            <i class="fas fa-sync-alt me-1"></i>تحديث البيانات
                        </button>
                        <a asp-action="Create" class="btn btn-success">
                            <i class="fas fa-plus-circle me-1"></i>إضافة نوع جديد
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Enhanced Success/Error Messages -->
@if (TempData["Success"] != null || TempData["Error"] != null)
{
    <div class="alert @(TempData["Success"] != null ? "alert-success" : "alert-danger") alert-dismissible fade show shadow-sm border-0" role="alert">
        <div class="d-flex align-items-center">
            <i class="fas @(TempData["Success"] != null ? "fa-check-circle" : "fa-exclamation-circle") me-3 fa-lg"></i>
            <div>
                <strong>@(TempData["Success"] != null ? "نجح!" : "خطأ!")</strong>
                <span class="ms-2">@(TempData["Success"] ?? TempData["Error"])</span>
            </div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<!-- Enhanced Data Display Section with Inline Search -->
<div id="stakeholderTypesContainer">
    <div class="section-card">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-light border-bottom">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="mb-0 fw-bold text-dark">
                        <i class="fas fa-list me-2 text-primary"></i>قائمة أنواع أصحاب المصلحة
                    </h5>
                    <div class="d-flex align-items-center gap-3">
                        <!-- Search Results Info -->
                        <small class="text-muted">
                            <span id="resultsInfo">عرض <strong id="visibleCount">0</strong> من أصل <strong id="totalCount">0</strong> نوع</span>
                        </small>
                        <div class="badge bg-warning px-3 py-2">
                            <i class="fas fa-database me-1"></i>
                            <span id="badgeCount">0</span> نوع
                        </div>
                        <button class="btn btn-outline-danger btn-sm" id="clearAllSearch" style="display: none;">
                            <i class="fas fa-eraser me-1"></i>مسح المرشحات
                        </button>
                    </div>
                </div>
            </div>

            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0" id="stakeholderTypesTable">
                        <thead class="table-light">
                            <tr>
                                <th class="fw-bold" style="width: 20%;">
                                    <i class="fas fa-tag text-primary me-1"></i>النوع
                                </th>
                                <th class="fw-bold" style="width: 20%;">
                                    <i class="fas fa-coins text-warning me-1"></i>نوع المعاملة المالية
                                </th>
                                <th class="fw-bold" style="width: 35%;">
                                    <i class="fas fa-link text-info me-1"></i>الواجهات المرتبطة
                                </th>
                                <th class="fw-bold" style="width: 15%;">
                                    <i class="fas fa-comment text-secondary me-1"></i>بيان
                                </th>
                                <th class="text-center fw-bold" style="width: 10%;">
                                    <i class="fas fa-cogs text-dark me-1"></i>الإجراءات
                                </th>
                            </tr>
                            <!-- Inline Search Row -->
                            <tr class="search-row bg-white">
                                <th class="border-bottom-0 pb-2 pt-2">
                                    <div class="search-container">
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light border-end-0">
                                                <i class="fas fa-search text-muted"></i>
                                            </span>
                                            <input type="text" id="searchByType" class="form-control border-start-0"
                                                   placeholder="ابحث في الأنواع...">
                                            <button class="btn btn-outline-secondary btn-sm" type="button" id="clearTypeSearch" style="display: none;">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </th>
                                <th class="border-bottom-0 pb-2 pt-2">
                                    <div class="search-container">
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light border-end-0">
                                                <i class="fas fa-search text-muted"></i>
                                            </span>
                                            <input type="text" id="searchByFinancial" class="form-control border-start-0"
                                                   placeholder="ابحث في المعاملات...">
                                            <button class="btn btn-outline-secondary btn-sm" type="button" id="clearFinancialSearch" style="display: none;">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </th>
                                <th class="border-bottom-0 pb-2 pt-2">
                                    <div class="search-container">
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light border-end-0">
                                                <i class="fas fa-search text-muted"></i>
                                            </span>
                                            <input type="text" id="searchByForms" class="form-control border-start-0"
                                                   placeholder="ابحث في الواجهات...">
                                            <button class="btn btn-outline-secondary btn-sm" type="button" id="clearFormsSearch" style="display: none;">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </th>
                                <th class="border-bottom-0 pb-2 pt-2">
                                    <div class="search-container">
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light border-end-0">
                                                <i class="fas fa-search text-muted"></i>
                                            </span>
                                            <input type="text" id="searchByComment" class="form-control border-start-0"
                                                   placeholder="ابحث في البيان...">
                                            <button class="btn btn-outline-secondary btn-sm" type="button" id="clearCommentSearch" style="display: none;">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </th>
                                <th class="border-bottom-0 pb-2 pt-2 text-center">
                                    <small class="text-muted">
                                        <i class="fas fa-filter me-1"></i>البحث والتصفية
                                    </small>
                                </th>
                            </tr>
                        </thead>
                        <tbody id="stakeholderTypesTableBody">
                            @foreach (var type in Model)
                            {
                                <tr class="stakeholder-type-row" data-id="@type.Id">
                                    <td class="stakeholder-type-name">
                                        <div class="d-flex align-items-center">
                                            <div class="type-icon me-2">
                                                <i class="fas fa-tag text-warning"></i>
                                            </div>
                                            <div>
                                                <span class="fw-semibold">@type.Type</span>
                                                <br>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="stakeholder-type-financial">
                                        @if (type.FinancialTransactionType != null)
                                        {
                                            <span class="badge bg-info bg-opacity-10 text-info border border-info border-opacity-25">
                                                <i class="fas fa-coins me-1"></i>@type.FinancialTransactionType.Type
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="text-muted fst-italic">
                                                <i class="fas fa-minus me-1"></i>لا يوجد نوع معاملة
                                            </span>
                                        }
                                    </td>
                                    <td class="stakeholder-type-forms">
                                        @if (type.StakeholderTypeForms?.Any() == true)
                                        {
                                            <div class="d-flex flex-wrap gap-1">
                                                @foreach (var form in type.StakeholderTypeForms)
                                                {
                                                    <span class="badge bg-light text-dark border border-secondary border-opacity-25">
                                                        <i class="fas fa-link me-1"></i>@form.Form.FormName
                                                    </span>
                                                }
                                            </div>
                                        }
                                        else
                                        {
                                            <span class="text-muted fst-italic">
                                                <i class="fas fa-unlink me-1"></i>لا توجد واجهات مرتبطة
                                            </span>
                                        }
                                    </td>
                                    <td class="stakeholder-type-comment">
                                        @if (string.IsNullOrEmpty(type.Comment))
                                        {
                                            <span class="text-muted fst-italic">
                                                <i class="fas fa-info-circle me-1"></i>لا يوجد بيان
                                            </span>
                                        }
                                        else
                                        {
                                            <span title="@type.Comment" class="comment-text">
                                                @(type.Comment.Length > 30 ? type.Comment.Substring(0, 27) + "..." : type.Comment)
                                            </span>
                                        }
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@type.Id"
                                               class="btn btn-sm btn-outline-info"
                                               title="تعديل @type.Type">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <button class="btn btn-sm btn-outline-secondary view-details"
                                                    data-id="@type.Id"
                                                    title="تفاصيل @type.Type">
                                                <i class="fas fa-info-circle"></i>
                                            </button>
                                            <a asp-action="Delete" asp-route-id="@type.Id"
                                               class="btn btn-sm btn-outline-danger"
                                               title="حذف @type.Type">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- No Results Message -->
    <div id="noResultsMessage" class="text-center py-5" style="display: none;">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <i class="fas fa-search fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">لا توجد نتائج مطابقة</h5>
                <p class="text-muted mb-3">لم يتم العثور على أنواع أصحاب مصلحة تطابق معايير البحث الحالية</p>
                <button class="btn btn-outline-primary" id="resetSearch">
                    <i class="fas fa-refresh me-1"></i>إعادة تعيين البحث
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Empty State Message -->
<div id="emptyState" class="text-center py-5" style="@(Model.Any() ? "display: none;" : "")">
    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <i class="fas fa-user-tag fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">لا توجد أنواع أصحاب مصلحة</h5>
            <p class="text-muted mb-3">لا توجد أنواع أصحاب مصلحة مضافة بعد في النظام</p>
            <a asp-action="Create" class="btn btn-success">
                <i class="fas fa-plus-circle me-1"></i>إضافة نوع جديد
            </a>
        </div>
    </div>
</div>

<!-- Enhanced Details Modal -->
<div class="modal fade" id="detailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-info text-white border-bottom-0">
                <h5 class="modal-title fw-bold">
                    <i class="fas fa-info-circle me-2"></i>تفاصيل نوع صاحب المصلحة
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-0" id="detailsContent">
                <!-- Details content will be loaded here -->
            </div>
            <div class="modal-footer bg-light border-top">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-1"></i>إغلاق
                </button>
                <button type="button" class="btn btn-info" id="editFromDetails">
                    <i class="fas fa-edit me-1"></i>تعديل
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Enhanced Toast Notifications -->
<div class="toast-container position-fixed bottom-0 end-0 p-3">
    <div id="notificationToast" class="toast align-items-center text-white border-0" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
            <div class="toast-body">
                <i id="toastIcon" class="fas fa-check-circle me-2"></i>
                <span id="toastMessage"></span>
            </div>
            <button type="button" class="btn-close btn-close-white ms-auto me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            const CURRENT_TIME = '2025-08-19 19:31:55';
            const CURRENT_USER = 'Ammar-Yasser8';

            console.log('Enhanced stakeholder types with inline search loaded:', {
                time: CURRENT_TIME,
                user: CURRENT_USER
            });

            // Initialize components
            const toastElement = document.getElementById('notificationToast');
            const toast = new bootstrap.Toast(toastElement, { delay: 4000 });
            const detailsModal = new bootstrap.Modal(document.getElementById('detailsModal'));

            let currentDetailsId = null;

            // Enhanced notification function
            function showNotification(message, type = 'success') {
                const icons = {
                    success: 'fa-check-circle',
                    error: 'fa-exclamation-circle',
                    warning: 'fa-exclamation-triangle',
                    info: 'fa-info-circle'
                };

                const colors = {
                    success: 'bg-success',
                    error: 'bg-danger',
                    warning: 'bg-warning',
                    info: 'bg-info'
                };

                $('#toastIcon').removeClass().addClass(`fas ${icons[type]} me-2`);
                $('#toastMessage').text(message);
                $('#notificationToast').removeClass('bg-success bg-danger bg-warning bg-info text-white text-dark')
                    .addClass(`${colors[type]} ${type === 'warning' ? 'text-dark' : 'text-white'}`);

                toast.show();
            }

            // Enhanced search functionality
            function performSearch() {
                const typeSearch = $('#searchByType').val().toLowerCase().trim();
                const financialSearch = $('#searchByFinancial').val().toLowerCase().trim();
                const formsSearch = $('#searchByForms').val().toLowerCase().trim();
                const commentSearch = $('#searchByComment').val().toLowerCase().trim();
                let visibleCount = 0;

                $('.stakeholder-type-row').each(function() {
                    const row = $(this);
                    const type = row.find('.stakeholder-type-name').text().toLowerCase();
                    const financial = row.find('.stakeholder-type-financial').text().toLowerCase();
                    const forms = row.find('.stakeholder-type-forms').text().toLowerCase();
                    const comment = row.find('.stakeholder-type-comment').text().toLowerCase();

                    const typeMatch = !typeSearch || type.includes(typeSearch);
                    const financialMatch = !financialSearch || financial.includes(financialSearch);
                    const formsMatch = !formsSearch || forms.includes(formsSearch);
                    const commentMatch = !commentSearch || comment.includes(commentSearch);

                    const shouldShow = typeMatch && financialMatch && formsMatch && commentMatch;
                    row.toggle(shouldShow);

                    if (shouldShow) visibleCount++;
                });

                updateSearchInfo(visibleCount, typeSearch, financialSearch, formsSearch, commentSearch);
            }

            function updateSearchInfo(visibleCount, typeSearch, financialSearch, formsSearch, commentSearch) {
                $('#visibleCount').text(visibleCount);
                $('#badgeCount').text(visibleCount);

                // Show/hide clear buttons
                $('#clearTypeSearch').toggle(typeSearch.length > 0);
                $('#clearFinancialSearch').toggle(financialSearch.length > 0);
                $('#clearFormsSearch').toggle(formsSearch.length > 0);
                $('#clearCommentSearch').toggle(commentSearch.length > 0);

                const hasAnySearch = typeSearch.length > 0 || financialSearch.length > 0 ||
                                   formsSearch.length > 0 || commentSearch.length > 0;
                $('#clearAllSearch').toggle(hasAnySearch);

                // Show/hide no results
                if (visibleCount === 0 && hasAnySearch) {
                    $('#noResultsMessage').show();
                    $('#stakeholderTypesContainer .section-card').hide();
                } else {
                    $('#noResultsMessage').hide();
                    $('#stakeholderTypesContainer .section-card').show();
                }

                // Add visual feedback to search inputs
                $('#searchByType').toggleClass('border-primary', typeSearch.length > 0);
                $('#searchByFinancial').toggleClass('border-primary', financialSearch.length > 0);
                $('#searchByForms').toggleClass('border-primary', formsSearch.length > 0);
                $('#searchByComment').toggleClass('border-primary', commentSearch.length > 0);
            }

            function updateCounts() {
                const totalCount = $('.stakeholder-type-row').length;
                const visibleCount = $('.stakeholder-type-row:visible').length;
                $('#totalCount').text(totalCount);
                $('#visibleCount').text(visibleCount);
                $('#badgeCount').text(visibleCount);
            }

            // Search events
            let searchTimeout;
            $('#searchByType, #searchByFinancial, #searchByForms, #searchByComment').on('input', function() {
                clearTimeout(searchTimeout);
                searchTimeout = setTimeout(performSearch, 200);
            });

            // Clear search buttons
            $('#clearTypeSearch').click(function() {
                $('#searchByType').val('').removeClass('border-primary');
                performSearch();
                $('#searchByType').focus();
            });

            $('#clearFinancialSearch').click(function() {
                $('#searchByFinancial').val('').removeClass('border-primary');
                performSearch();
                $('#searchByFinancial').focus();
            });

            $('#clearFormsSearch').click(function() {
                $('#searchByForms').val('').removeClass('border-primary');
                performSearch();
                $('#searchByForms').focus();
            });

            $('#clearCommentSearch').click(function() {
                $('#searchByComment').val('').removeClass('border-primary');
                performSearch();
                $('#searchByComment').focus();
            });

            $('#clearAllSearch, #resetSearch').click(function() {
                $('#searchByType, #searchByFinancial, #searchByForms, #searchByComment').val('').removeClass('border-primary');
                performSearch();
            });

            // View details handler
            $(document).on('click', '.view-details', function() {
                const id = $(this).data('id');
                loadStakeholderTypeDetails(id);
            });

            // Load stakeholder type details (mock function - implement API when available)
            function loadStakeholderTypeDetails(id) {
                // Find the row data
                const row = $(`.stakeholder-type-row[data-id="${id}"]`);
                const typeName = row.find('.stakeholder-type-name .fw-semibold').text();
                const financialType = row.find('.stakeholder-type-financial').text().trim();
                const forms = row.find('.stakeholder-type-forms .badge').map(function() {
                    return $(this).text().replace(/.*\s/, ''); // Remove icon text
                }).get();
                const comment = row.find('.stakeholder-type-comment').text().trim();

                renderDetailsModal({
                    id: id,
                    type: typeName,
                    financialType: financialType.includes('لا يوجد') ? null : financialType,
                    forms: forms,
                    comment: comment.includes('لا يوجد') ? null : comment
                });

                currentDetailsId = id;
                detailsModal.show();
            }

            // Render details modal content
            function renderDetailsModal(data) {
                const formsHtml = data.forms && data.forms.length > 0
                    ? data.forms.map(form =>
                        `<div class="col-auto">
                            <span class="badge bg-light text-dark border border-secondary border-opacity-25 fs-6 py-2 px-3">
                                <i class="fas fa-link me-1"></i>${form}
                            </span>
                        </div>`
                      ).join('')
                    : '<div class="col-12"><span class="text-muted fst-italic"><i class="fas fa-unlink me-2"></i>لا توجد واجهات مرتبطة</span></div>';

                const financialHtml = data.financialType
                    ? `<div class="alert alert-light border-start border-4 border-info">
                        <h6 class="alert-heading mb-2">
                            <i class="fas fa-coins text-info me-2"></i>نوع المعاملة المالية
                        </h6>
                        <p class="mb-0">${data.financialType}</p>
                       </div>`
                    : '<div class="alert alert-light text-muted text-center"><i class="fas fa-info-circle me-2"></i>لا يوجد نوع معاملة مالية محدد</div>';

                const commentHtml = data.comment
                    ? `<div class="alert alert-light border-start border-4 border-warning">
                        <h6 class="alert-heading mb-2">
                            <i class="fas fa-comment-alt text-warning me-2"></i>البيان والتعليقات
                        </h6>
                        <p class="mb-0">${data.comment}</p>
                       </div>`
                    : '<div class="alert alert-light text-muted text-center"><i class="fas fa-comment-slash me-2"></i>لا يوجد بيان أو تعليقات</div>';

                const detailsContent = `
                    <div class="p-4">
                        <!-- Header Section -->
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="d-flex align-items-center p-3 bg-light rounded">
                                    <div class="flex-shrink-0 me-3">
                                        <div class="icon-circle bg-warning text-white">
                                            <i class="fas fa-user-tag fa-2x"></i>
                                        </div>
                                    </div>
                                    <div class="flex-grow-1">
                                        <h4 class="mb-1 fw-bold text-dark">${data.type}</h4>
                                        <div class="row">
                                            <div class="col-md-6">
                                                <small class="text-muted">
                                                    <i class="fas fa-hashtag me-1"></i>معرف النوع: <strong>#${data.id}</strong>
                                                </small>
                                            </div>
                                            <div class="col-md-6">
                                                <small class="text-muted">
                                                    <i class="fas fa-link me-1"></i>عدد الواجهات: <strong>${data.forms ? data.forms.length : 0}</strong>
                                                </small>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Financial Transaction Type Section -->
                        <div class="row mb-4">
                            <div class="col-12">
                                ${financialHtml}
                            </div>
                        </div>

                        <!-- Forms Section -->
                        <div class="row mb-4">
                            <div class="col-12">
                                <h6 class="fw-bold text-dark mb-3">
                                    <i class="fas fa-link text-info me-2"></i>الواجهات المرتبطة
                                </h6>
                                <div class="row g-2">
                                    ${formsHtml}
                                </div>
                            </div>
                        </div>

                        <!-- Comment Section -->
                        <div class="row">
                            <div class="col-12">
                                ${commentHtml}
                            </div>
                        </div>

                        <!-- Meta Information -->
                        <div class="row mt-4 pt-3 border-top">
                            <div class="col-12">
                                <div class="row text-center">
                                    <div class="col-md-4">
                                        <div class="p-2">
                                            <div class="text-primary fs-5 fw-bold">${data.forms ? data.forms.length : 0}</div>
                                            <small class="text-muted">واجهات مرتبطة</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                `;

                $('#detailsContent').html(detailsContent);
            }

            // Edit from details modal
            $('#editFromDetails').click(function() {
                if (currentDetailsId) {
                    detailsModal.hide();
                    window.location.href = `/StakeholderType/Edit/${currentDetailsId}`;
                }
            });

            // Refresh data
            $('#refreshData').click(function() {
                const btn = $(this);
                const originalText = btn.html();
                btn.prop('disabled', true).html('<span class="spinner-border spinner-border-sm me-2"></span>جاري التحديث...');

                setTimeout(() => {
                    location.reload();
                }, 1000);
            });

            // Auto-dismiss success alerts after 5 seconds
            setTimeout(function() {
                $('.alert-success, .alert-danger').fadeOut(350);
            }, 5000);

            // Keyboard shortcuts
            $(document).keydown(function(e) {
                if (e.ctrlKey && e.keyCode === 70) { // Ctrl+F
                    e.preventDefault();
                    $('#searchByType').focus();
                }
                if (e.keyCode === 27) { // Escape
                    $('#searchByType, #searchByFinancial, #searchByForms, #searchByComment').val('').removeClass('border-primary');
                    performSearch();
                }
            });

            // Initialize page
            updateCounts();

            console.log('Enhanced stakeholder types page with inline search initialized successfully at', CURRENT_TIME, 'by', CURRENT_USER);
        });
    </script>

    <style>
        /* Enhanced Styles */
        .icon-circle {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .stakeholder-type-row {
            transition: all 0.2s ease;
        }

            .stakeholder-type-row:hover {
                background-color: rgba(255,193,7,0.05);
                transform: scale(1.002);
            }

        .search-row th {
            background-color: #ffffff;
            border-bottom: 1px solid #dee2e6;
            padding: 8px 12px;
        }

        .search-container .input-group {
            transition: all 0.2s ease;
        }

            .search-container .input-group:hover {
                transform: translateY(-1px);
            }

            .search-container .input-group.shadow-sm {
                box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075) !important;
            }

        .search-container .input-group-text {
            background-color: #f8f9fa;
            border-color: #ced4da;
            font-size: 0.875rem;
        }

        .search-container .form-control {
            border-color: #ced4da;
            font-size: 0.875rem;
            padding: 6px 12px;
        }

            .search-container .form-control:focus {
                border-color: #86b7fe;
                box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
            }

            .search-container .form-control.border-primary {
                border-color: #0d6efd !important;
                box-shadow: 0 0 0 0.1rem rgba(13, 110, 253, 0.15);
            }

        .type-icon {
            width: 30px;
            text-align: center;
        }

        .btn-group .btn {
            margin: 0 1px;
        }

        .comment-text {
            cursor: help;
        }

        .toast {
            min-width: 300px;
        }

        .modal-content {
            border-radius: 12px;
        }

        /* Enhanced details modal styling */
        .details-modal .icon-circle {
            width: 80px;
            height: 80px;
            font-size: 2rem;
        }

        .border-start {
            border-left: 4px solid !important;
        }

        .stakeholder-type-row {
            animation: fadeInUp 0.5s ease-out;
        }

        @@keyframes fadeInUp {
            from

        {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .table thead th {
            position: relative;
            vertical-align: middle;
        }

        .table thead tr:first-child th {
            border-bottom: 2px solid #dee2e6;
        }

        /* Mobile responsiveness */
        @@media (max-width: 768px) {
            .icon-circle

        {
            width: 45px;
            height: 45px;
            font-size: 1.2rem;
        }

        .btn-group {
            flex-direction: column;
            width: 100%;
        }

            .btn-group .btn {
                margin: 1px 0;
                border-radius: 4px !important;
            }

        .search-row th {
            padding: 6px 8px;
        }

        .search-container .form-control {
            font-size: 0.8rem;
            padding: 4px 8px;
        }

        .details-modal .icon-circle {
            width: 60px;
            height: 60px;
            font-size: 1.5rem;
        }

        }

        /* Print styles */
        @@media print {
            .btn-group, .search-row

        {
            display: none !important;
        }

        .stakeholder-type-row {
            break-inside: avoid;
        }

        }
    </style>
}